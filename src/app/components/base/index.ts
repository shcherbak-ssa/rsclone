import { BaseButtonProps, Button } from './button';
import { BaseInputProps, Input } from './input';
import { BaseSelectProps, Select } from './select';

export const Base = {
  Button,
  Input,
  Select,
};

export {
  BaseButtonProps,
  BaseInputProps,
  BaseSelectProps,
}
