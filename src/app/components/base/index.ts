import { BaseButtonProps, Button } from './button';
import { BaseInputProps, Input } from './input';
import { BaseSelectProps, Select } from './select';
import { BaseRadioProps, Radio } from './radio';

export const Base = {
  Button,
  Input,
  Select,
  Radio,
};

export {
  BaseButtonProps,
  BaseInputProps,
  BaseSelectProps,
  BaseRadioProps,
}
