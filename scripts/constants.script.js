const DIST_DIRNAME = 'dist';
const LANGUAGES_DIRNAME = 'languages';
const PUGLIC_BUILD_DIRNAME = 'public';
const SERVER_BUILD_DIRNAME = 'server';

module.exports = {
  DIST_DIRNAME,
  LANGUAGES_DIRNAME,
  PUGLIC_BUILD_DIRNAME,
  SERVER_BUILD_DIRNAME,
};
